version: "3.2"
services:
  server:
    ports:
      - 22:22
    build:
      context: ./
      dockerfile: ./server/Dockerfile
  
  bastion:
    ports:
      - 22:22
    build:
      context: ./
      dockerfile: ./bastion-host/Dockerfile

  tester:
    build:
      context: ../
      dockerfile: ./test/client/Dockerfile
    depends_on:
      - server

